# JSON to HTML Page Exercise

- Given a JSON object, build a page of items from that JSON that meets specific design parameters (items per row, the color of title, border, margins, padding all according to spec).
